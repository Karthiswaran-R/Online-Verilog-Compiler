{
  "version": 2,
  "builds": [
    {
      "src": "app.py",  // Change this to the main file of your project
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/app.py"  // Route all traffic to your app.py
    }
  ]
}
